# Руководство по Деплою на Google Cloud Platform (GCP)

Это руководство поможет вам развернуть проект **QuantPulse** на сервере Ubuntu, используя автоматический скрипт настройки.

## Шаг 1: Подключение к серверу (SSH)

1. Откройте **Google Cloud Console**.
2. Перейдите в **Compute Engine** -> **VM instances**.
3. Найдите свою машину `quantpulse-server`.
4. Нажмите кнопку **SSH** в строке этой машины.
5. Откроется черное окно терминала прямо в браузере. Это и есть ваш сервер.

## Шаг 2: Установка (Все одной командой)

В окне терминала (SSH) выполните следующие команды по очереди:

### 1. Скачиваем репозиторий
```bash
git clone https://github.com/ArsenLabovich/QuantPulse.git
cd QuantPulse
```
*(Если репозиторий приватный, он попросит логин/пароль. Лучше использовать Token или сделать репозиторий публичным на 5 минут).*

### 2. Запускаем магический скрипт
```bash
cd deployment/gcp
chmod +x setup_gcp.sh
./setup_gcp.sh
```

---

## Что сделает скрипт?
1. **Установит Docker**: Самую свежую версию.
2. **Определит ваш IP**: Сам найдет внешний IP адрес сервера.
3. **Сгенерирует Домен**: Создаст адрес вида `quantpulse.34.11.22.33.nip.io`.
4. **Сгенерирует Пароли**: Создаст надежные пароли для базы данных.
5. **Запустит проект**: В режиме Production с HTTPS защитой.

## Шаг 3: Проверка

После того как скрипт напишет `🎉 Deployment Complete!`, подождите еще 30-60 секунд (пока запустится база данных).

Откройте ссылку, которую выдал скрипт (например `https://quantpulse.34.11.22.33.nip.io`).
Вы должны увидеть рабочий сайт с **Зеленым замочком** (HTTPS).
